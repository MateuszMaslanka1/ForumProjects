// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyC06ibcg8J6xdM4jHf3RuBTrHARsoi_Vck",
    authDomain: "dbforforum.firebaseapp.com",
    databaseURL: "https://dbforforum.firebaseio.com",
    projectId: "dbforforum",
    storageBucket: "dbforforum.appspot.com",
    messagingSenderId: "15727587372",
    appId: "1:15727587372:web:10932362da7eb417"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
